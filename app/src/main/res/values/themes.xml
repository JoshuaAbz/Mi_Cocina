<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MiCocina" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/azul</item>
        <item name="colorPrimaryVariant">@color/azul_oscuro</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MiCocina" parent="Base.Theme.MiCocina" />

    <style name="BottomNavigation">
        <item name="android:background">@color/azul_oscuro</item>
        <item name="itemIconTint">@color/azul_claro2</item>
        <item name="itemTextColor">@color/azul_claro2</item>
    </style>

    <style name="imgCircular">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <resources>
        <style name="estrellas">
            <item name="colorControlNormal">@android:color/darker_gray</item>
            <item name="colorControlActivated">@android:color/holo_orange_light</item>
            <item name="colorControlHighlight">@android:color/holo_orange_dark</item>
        </style>
    </resources>

</resources>